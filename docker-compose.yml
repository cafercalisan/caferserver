services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cafer
      POSTGRES_PASSWORD: caferserver123
      POSTGRES_DB: caferserver
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
